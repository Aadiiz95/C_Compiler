const fs = require('fs');

const packageJsonContent = {
  "name": "compiler",
  "version": "1.0.0",
  "main": "Api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "body-parser": "^1.20.2",
    "compilex": "^0.7.4",
    "express": "^4.19.2",
    "nodemon": "^3.1.4"
  }
};

fs.writeFile('versel.json', JSON.stringify(packageJsonContent, null, 2), (err) => {
  if (err) {
    console.error('Error writing file:', err);
  } else {
    console.log('File written successfully');
  }
});
